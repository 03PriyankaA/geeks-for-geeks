class Solution{
public:	
	vector<int> getMoreAndLess(int arr[], int n, int x) {
	    // code here
	    int small=0,lar=0;
	    for(int i=0;i<n;i++)
	    {
	    if(arr[i]<=x)
	    {
	        small++;
	    }
	  if(arr[i]>=x)
	  {
	      lar++;
	  }
	}
	return {small,lar};
	}
};
