class Solution{
public:	
	// swap k'th element from beginning and end
	void swapKth(int *arr, int n, int k) {
	    // code here
	    
	    int i=k-1,j=n-k;
	    int temp;
	    temp=arr[i];
	    arr[i]=arr[j];
	    arr[j]=temp;
	}
};
